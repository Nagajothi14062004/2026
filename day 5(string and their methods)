//first method:
       string str="innate talent";
//second method only using string:
     string str2=sc.nextLine();
//for float or other with string:
    sc.nextLine();
    string str2=sc.nextLine();
                                                  string methods
1.//charAt:
        return the character at the specified   index(char)
   EG:
       String str="innate talent";
       System.out.print(str.charAt(0));
2.//compareTo:
    compare two strings with its asci value and reduce the value when the string differ in their cases(int)
EG:
    String str="innate talent";
    String str1="Innate Talent";
    System.out.print(str.compareTo(str1));

3.//compareToIgnoreCAse():
           compare two strings but ignore space differences(int)
EG:
      String str="innate talent";
      String str1="Innate Talent";
      System.out.print(str.compareToIgnoreCase(str1));
4.//concat():
     append a string to the end of another string(string)
EG:
method1:
      String str="innate talent";
      String str1="Innate Talent";
      System.out.print(str.concat(str1));
method 2(using +):
      System.out.print(str+" "+str1);
5.//contains():
    chcek if the string contain a sequence of characters(boolean)
EG:
      String str="innate talent";
      String str1="innate ";
      System.out.print(str.contains(str1));
6.//indexOf():
    return the position of the first found occurence of specified characters in a string(int)
EG:
        String str="Innate Talent";
        System.out.print(str.indexOf('n'));
       System.out.println(str.indexOf("ate Tal"));
    
7.//isEmpty():
      check whether a string is empty or not(boolean)
EG:
         String str="Innate Talent";
        System.out.print(str.indexOf('n'));
        System.out.println(str.indexOf("ate Tal"));
    
8.//lastIndexOf():
             return the position of the last found occurence of specified characters in a string(int)
EG:
       String str="Innate Talent";
       System.out.println(str.lastIndexOf('t'));
       System.out.println(str.lastIndexOf("Talent"));
9.//length():
     return the length of string (int)

  EG:
      String str="innate talent";
      String str1="innate ";
      System.out.print(str1.length());
     
10.//equals:
     compare two strings return true if the strings are equal
EG:
       str="Innate Talent";
      String str1="and";
       System.out.println(str.equals(str1));
11.//equalIgnoreCase:
       compare two strings,ignoring case consideration
EG:
        String str="Innate Talent";
       String str1="innate talent";
       System.out.println(str.equalsIgnoreCase(str1));
12.//endswith:
        check whether a string ends with specified character
EG:
13.//replace:
    searches a string for a specified value,and return a new string where the specified values are replaced(string)
EG:
      Scanner sc=new Scanner(System.in);
      String str="innate ";
      System.out.println(str.replace("i","n"));
14.//replaceFirst:
         replace the first occurence of the substring that matches the given regular expression with the given replacement
EG:
   Scanner sc=new Scanner(System.in);
      String str="innate ";
      System.out.println(str.replaceFirst("n",","));
 15.//replaceall:
replace  each substring that matches the given regular expression with the given replacement
EG:
     Scanner sc=new Scanner(System.in);
      String str="innate ";
      System.out.println(str.replaceAll("[in]",","));
16.//split:
   it split the string into substring

EG:
       String str="today is friday";
      String arr[]=str.split("a");
      for(int i=0;i<arr.length;i++){
        System.out.println(arr[i]);
      }
17.toLowercase:
      convert a string to lowercase
EG:
        String str="innate talent";
      System.out.println(str.toLowerCase());

18.toUppercase:
      convert a string to upppercase
EG:
      String str="  innate talent  ";
      System.out.println(str.toUpperCase());
19.trim:
      remove whitespaces from both ends of a string

EG:
      String str="  innate talent  ";
      System.out.println(str.trim());
20.toString();
    return the  value  of a string object(string)
EG:
21.startsWIth:
       check whether a string starts with specified characters

EG:

22.substring:
     return a new string which is the substring of a specified a string 
EG:
       String str="innate talent";
       System.out.print(str.substring(3));
        System.out.print(str.substring(3,7));
23.toCharArray:
        convert this string to anew character array
